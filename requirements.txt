# Quorum Insights — Python dependencies
# Budget: <15 deps (per tech-choices.md)

# Core
pydantic>=2.0,<3.0
polars>=1.0,<2.0
httpx>=0.27,<1.0

# LLM (optional — only needed for insight generation)
anthropic>=0.40,<1.0
